x = float(input("What's x?: "))
y = float(input("What's y?: "))
z = x / y

# .2f = choose how many digits you want to pring
print(f"{z:.3f}")

# z = round(x + y)
#
# # Used fstring to return larger numbers with comma separator
# print(f"{z:,}")
